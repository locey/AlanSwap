## 安装依赖
```npm install```
## 运行测试
``` npx hardhat test ```
## 集成oracle获取价格，测试环境集成Mock

## PS
#### 目前还没有集成部署js

## 所有指令
1. 本地启动节点
``` npx hardhat node (--network localhost) 可不加，默认```
2. 连接本地环境
``` npx hardhat test --network localhost ```
